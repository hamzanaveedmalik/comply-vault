/**
 * ROI marketing section removed. Unsupported statistics are not claimed.
 * Kept as a no-op export so older imports do not break during the rebuild.
 */
export function ROIStats(): null {
  return null
}
